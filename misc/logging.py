# -*- coding: utf-8 -*-
import logging

logging.basicConfig(
                    format=u'%(filename)s-[LINE:%(lineno)d]-#%(levelname)-8s-[%(asctime)s]-%(message)s\n',
                    # level=logging.INFO,
                    level=logging.DEBUG,
                    )
